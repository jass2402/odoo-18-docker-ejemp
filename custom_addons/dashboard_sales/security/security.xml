<odoo>
    <data noupdate="1">
        <!-- Define a new group for Sales Dashboard -->
        <record id="model_dashboard_sales" model="ir.model">
            <field name="name">dashboard.sales</field>
            <field name="model">dashboard.sales</field>
        </record>

        <!-- Assign the group to the Sales Manager role 
        <record id="sales_manager_group" model="res.groups">
            <field name="name">Sales Manager</field>
            <field name="category_id" ref="base.module_category_sales_management"/>
            <field name="users" eval="[(4, ref('base.user_admin'))]"/>
            <field name="implied_ids" eval="[(4, ref('group_sales_dashboard'))]"/>
        </record> -->
    </data>
</odoo>